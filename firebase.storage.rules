rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    match /games/{game}/thumbnail/{fileName} {
      allow read: if true;
      allow write: if isLoggedIn()
    }

    function isLoggedIn() {
      return request.auth != null;
    }
  }
}
